<h1>Eat-A-Something</h1>

<h2>Food to Eat:</h2>

<ul>
{{#each foods}}
	{{#unless devoured}}
		{{> food/food-block devoured=true}}
	{{/unless}}
{{/each}}
</ul>

<h2>Devoured:</h2>

<ul>
{{#each foods}}
	{{#if devoured}}
		{{> food/food-block devoured=false}}
	{{/if}}
{{/each}}
</ul>

<h2>Add a Food</h2>

<form class="create-form">

	<div class="form-group">
		<label for="re">Restaurant:</label>
		<input type="text" id="re" name="restaurant">
	</div>

	<div class="form-group">
		<label for="ca">Food Name:</label>
		<input type="text" id="ca" name="foodItem">
	</div>

	<button class ="btn btn-primary" type="submit">Add Food</button>

</form>
